import Vue from 'vue';
import {{data.name | camelCase}}Component from './index.vue';

// Here are some Jasmine 2.0 tests, though you can
// use any test runner / assertion library combo you prefer
describe('{{data.name | camelCase}}Component', () => {
  // Inspect the raw component options
  it('has a created hook', () => {
    // expect(typeof {{data.name | camelCase}}Component.created).toBe('function');
  });
});
